[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "nnue-loader"
version = "0.1.0"
description = "NNUE training data loader"
requires-python = ">=3.10"

[tool.maturin]
python-source = "python"
module-name = "nnue_loader._internal"
